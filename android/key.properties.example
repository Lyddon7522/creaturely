# Copy this file to android/key.properties and keep the copy private.
# Relative storeFile paths are resolved from android/app.
storeFile=../upload-keystore.jks
storePassword=replace-with-keystore-password
keyAlias=upload
keyPassword=replace-with-key-password
